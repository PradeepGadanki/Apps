export const fr = (l) => {
    let arr;
    for (let i = 0; i < l; i++) {
        const b = Math.floor(Math.random() * (130 - 70 + 1)) + 70;
        arr = b
    }
    console.log('utils arr', arr)
    return arr
}